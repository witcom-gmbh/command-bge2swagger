app:
  command:
    base-url: ${CMD_BASE_URL}
  generator:
    output-dir: ${GENERATOR_OUTPUT_DIR:/tmp}    
    generate-enums: ${GENERATOR_GENERATE_ENUM:false}
    sessionid-as-queryparam: false

logging:
  level:
    root: ERROR
    de.witcom: DEBUG
       